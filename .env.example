# Environment Configuration for Data Cleaning OpenEnv
# Copy this to .env and fill in your actual values

# ============================================================================
# MANDATORY ENVIRONMENT VARIABLES (for hackathon submission)
# ============================================================================

# API endpoint for LLM (defaults to localhost:7860 for testing)
API_BASE_URL=https://api.openai.com/v1

# Model to use for inference (use smaller models to minimize cost)
MODEL_NAME=gpt-4o-mini

# Your API key (OpenAI, HuggingFace, or other compatible API)
# IMPORTANT: No default - you MUST set this for LLM inference
HF_TOKEN=your_api_key_here

# ============================================================================
# OPTIONAL VARIABLES
# ============================================================================

# Environment URL (where the data cleaning environment is running)
ENV_URL=http://localhost:7860

# ============================================================================
# USAGE NOTES
# ============================================================================
# 
# For OpenAI API:
#   API_BASE_URL=https://api.openai.com/v1
#   MODEL_NAME=gpt-4o-mini  (cheapest, ~$0.15/1M tokens)
#   HF_TOKEN=sk-...         (your OpenAI API key)
#
# For HuggingFace Inference:
#   API_BASE_URL=https://api-inference.huggingface.co/v1
#   MODEL_NAME=meta-llama/Llama-3.2-1B-Instruct
#   HF_TOKEN=hf_...         (your HF token)
#
# For local testing without LLM:
#   Use test_inference_format.py (rule-based actions)
